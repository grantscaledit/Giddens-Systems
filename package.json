{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check:copy": "! grep -rn '\\[\\[PH:' src/ --include='*.astro' || (echo '\\nPLACEHOLDER COPY FOUND — cannot ship\\n' && exit 1)",
    "build": "npm run check:copy && astro build && rm -rf dist/dev",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/instrument-sans": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "astro": "^7.1.6"
  }
}
